import React from 'react';

const Home = () => (
  <div className="container">
    <div className="jumbotron" style={{ textAlign: 'center' }}>
      Home Page
    </div>
  </div>
);

export default Home;
