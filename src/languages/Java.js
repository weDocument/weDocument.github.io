import React from 'react';

const Java = () => (
  <div className="container">
    <div
      className="jumbotron"
      style={{ textAlign: 'center', marginTop: '100px' }}
    >
      Java Page
    </div>
  </div>
);

export default Java;
