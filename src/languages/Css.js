import React from 'react';
import { Container } from 'react-bootstrap';

const Css = () => (
  <Container>
    <div
      className="jumbotron"
      style={{ textAlign: 'center', marginTop: '100px' }}
    >
      Css Page
    </div>
  </Container>
);

export default Css;
